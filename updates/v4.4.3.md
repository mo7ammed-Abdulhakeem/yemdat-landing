# v4.4.3 — Email Fix Bundle (2026-04-22)

## What changed

### Account Deletion Email — Complete Fix
- `AccountDeletionOtpEmail` was the only mailable not using the dynamic email system.
  The Arabic subject was corrupted to `?????` because it was hardcoded in the PHP source file.
  Now loads template from the database like all other emails.
- `AccountDeletionController` updated to pass `{name}` and `{otp}` placeholders correctly.
- New `AccountDeletionOtpEmail` email template added to the database (English + Arabic).

### Event Confirmation — Calendar Attachment Fixes
- **"Unable to launch event" error fixed**: the `.ics` file was including an empty `URL:` line
  when no video call link was set. Removed when not applicable.
- **Correct event title in ICS**: title now uses the user's locale (Arabic/English).
- **Attachment renamed**: `event.ics` → `add-to-calendar.ics` (English) / `اضافة-للتقويم.ics` (Arabic).

### Event Confirmation Email Subject — Placeholder Fix
- On production the email subject was showing `{title_en}` unresolved because the database
  template was created before placeholder names were standardized.
- A migration now updates the template automatically on `php artisan migrate`.

## Files changed
- `app/Http/Controllers/AccountDeletionController.php`
- `app/Mail/AccountDeletionOtpEmail.php`
- `app/Mail/EventConfirmationEmail.php`
- `database/seeders/EmailTemplateSeeder.php`
- `database/migrations/2026_04_22_000001_fix_event_confirmation_email_template.php` *(new)*

## Server deployment
1. Upload all 5 files above
2. `php artisan migrate`
3. `php artisan db:seed --class=EmailTemplateSeeder`
4. `php artisan config:clear && php artisan cache:clear`
